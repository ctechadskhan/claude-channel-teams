{
  "name": "claude-channel-teams",
  "version": "0.0.1",
  "license": "MIT",
  "type": "module",
  "bin": "./src/server.ts",
  "description": "Microsoft Teams channel plugin for Claude Code (Phase 1 — scaffold only).",
  "scripts": {
    "start": "bun install --no-summary && bun src/server.ts",
    "typecheck": "bun tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "botbuilder": "^4.22.0",
    "restify": "^11.1.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/restify": "^8.5.12",
    "typescript": "^5.5.0"
  }
}
