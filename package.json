{
  "name": "claude-channel-teams",
  "version": "0.3.0",
  "license": "MIT",
  "type": "module",
  "bin": "./src/server.ts",
  "description": "Microsoft Teams channel plugin for Claude Code — bridge a Teams bot into a running Claude Code session, with pairing flow and tool-approval relay.",
  "scripts": {
    "start": "bun install --no-summary && bun src/server.ts",
    "typecheck": "bun tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "botbuilder": "^4.22.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.5.0"
  }
}
